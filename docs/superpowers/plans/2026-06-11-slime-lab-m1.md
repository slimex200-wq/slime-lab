# SLIME LAB M1 — 뼈대 출하 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v8 프로토타입의 점묘 심장 히어로를 Astro 모듈로 이식하고, 표본 인덱스 + 검사실 스텁과 함께 Cloudflare Pages에 배포 가능한 상태로 만든다.

**Architecture:** Astro(정적, zero-JS 기본) + 순수 TS 엔진 모듈(`src/engine/`, 프레임워크 무관). 엔진의 순수 함수(박동 곡선, BPM 매핑, 해부 샘플링, 물리)는 vitest로 TDD, WebGL 렌더/오케스트레이션은 브라우저 데몬 스크린샷으로 검증. 데이터는 M1에서 정적 JSON(스키마는 M2 라이브 데이터와 동일).

**Tech Stack:** Astro **5 (^5로 핀 — 최신 major는 6이므로 주의)** + TypeScript strict, vitest, vanilla WebGL1, @fontsource(셀프호스팅 폰트), Cloudflare Pages.

> **2026-06-11 적대 리뷰 반영 (Codex + 독립 검증자, 11건):** Astro ^5 핀 / create-astro 플래그 정정 / ClientRouter·sound는 스펙대로 M2·M3로 이연 / 표본 12→16 / vitals 16키 + stale 로직은 M1에서 "DATA AS OF" 라벨로 대체 / `git commit -am`→`git add -A` / vitest `--passWithNoTests` / `@astrojs/check`+`typescript` 설치 / Lighthouse 경로 repo-local / anatomy.col(기본색 float)↔renderer col(동적 Uint8) 계약 명시.

**SoT:** `docs/superpowers/specs/2026-06-11-slime-lab-design.md`
**엔진 레퍼런스:** `.superpowers/brainstorm/1195-1781181020/content/hero-alive-v8.html` (이하 "v8"). v8의 `<script>`는 기능별 `/* ═══ 제목 ═══ */` 주석 블록으로 구획되어 있다 — 각 태스크가 어느 블록을 이식하는지 명시한다.

**작업 디렉터리:** `C:\Users\slime\claude-projects\lab` (git 초기화 완료, docs/ 커밋 존재)

---

## 파일 구조 (최종)

```
src/
├─ engine/
│  ├─ rng.ts            시드 RNG (테스트 재현성)
│  ├─ heart-state.ts    HeartState, beatCurve, bpmFromCommits, stepHeart
│  ├─ anatomy.ts        CSG 프리미티브 + 30k 점 샘플링 (v8 "해부학적 심장" 블록)
│  ├─ physics.ts        스프링/커서반발/쇼크 (v8 메인 루프 물리 부분)
│  ├─ renderer.ts       WebGL POINTS 렌더러 (v8 "WebGL" 블록)
│  ├─ ecg.ts            ECG 캔버스 (v8 "ECG" 부분)
│  ├─ overlay.ts        콜아웃 리더라인 + 먼지 + 레일 클램프 (v8 "주석 캔버스"/"레일")
│  ├─ cursor.ts         커스텀 커서 + 자석 버튼 (v8 "커서"/"자석")
│  └─ mount.ts          오케스트레이터 — mount(host, specimens, vitals)  ※ sound는 M3
├─ content.config.ts    표본 콘텐츠 컬렉션 스키마
├─ content/specimens/   *.md 표본 16개
├─ data/vitals.json     정적 바이탈 (M2 라이브 스키마와 동일)
├─ lib/vitals.ts        getVitals() — stale 플래그 포함
├─ styles/global.css    토큰/격자 배경/타이포/공용 UI
├─ layouts/Base.astro
└─ pages/
   ├─ index.astro       §1 HERO + §2 SPECIMEN INDEX
   └─ specimen/[slug].astro  검사실 스텁 (M2에서 확장)
tests/engine/*.test.ts
public/fallback-heart.png   WebGL 미지원 폴백
```

---

### Task 1: Astro 스캐폴드 + vitest + 폰트

**Files:** Create: `package.json`, `astro.config.mjs`, `tsconfig.json`, `vitest.config.ts`

- [ ] **Step 1: Astro 프로젝트 생성 (기존 디렉터리에)**

```bash
cd C:\Users\slime\claude-projects\lab
npm create astro@latest . -- --template minimal --no-install --no-git
```
(`--typescript` 플래그는 현행 create-astro에 없음 — tsconfig는 Step 2에서 strict로 직접 설정.) 프롬프트가 "directory not empty"를 물으면 기존 파일 유지 옵션 선택. 생성 직후 `package.json`의 `astro`를 `"^5.0.0"`으로 핀 (최신 major 6 설치 방지).

- [ ] **Step 2: 의존성 설치 + tsconfig strict**

```bash
npm i
npm i -D vitest @astrojs/check typescript
npm i @fontsource/geist-sans @fontsource/geist-mono @fontsource-variable/newsreader
```
`tsconfig.json`이 `"extends": "astro/tsconfigs/strict"`인지 확인, 아니면 변경.

- [ ] **Step 3: `vitest.config.ts` 작성**

```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({ test: { include: ['tests/**/*.test.ts'] } });
```

`package.json` scripts에 추가: `"test": "vitest run --passWithNoTests", "test:watch": "vitest"`

- [ ] **Step 4: 빌드/테스트 스모크**

Run: `npm run build` → Expected: dist/ 생성, exit 0
Run: `npm test` → Expected: exit 0 (`--passWithNoTests` — 테스트 0개 상태에서 vitest 기본은 exit 1이므로 필수)

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "chore: Astro 5 + vitest + self-hosted fonts 스캐폴드"
```

---

### Task 2: 디자인 토큰 + 글로벌 스타일 + 베이스 레이아웃

**Files:** Create: `src/styles/global.css`, `src/layouts/Base.astro`

- [ ] **Step 1: `src/styles/global.css`**

v8 `<style>`에서 다음을 이식 (셀렉터 이름 유지): 토큰(`:root`에 `--ink:#050807; --mint:#2BE4A4; --mint-dim:#3E544B; --red:#D9485A` 등), `#grid-bg`(그래프페이퍼 격자 + 비네트), `.topbar`, `.btn`, `.headline`, `.vitals`, `.vrow`, `.frame`, `#rail` 계열, `.callout`, `.srow` 계열, `.reveal`/`@keyframes rv`/`blip`, 커서(`#cur-dot`, `#cur-ring`), 미디어쿼리 2개. 폰트는 @fontsource import로 대체:

```css
@import '@fontsource/geist-sans/400.css';
@import '@fontsource/geist-sans/500.css';
@import '@fontsource/geist-sans/600.css';
@import '@fontsource/geist-mono/400.css';
@import '@fontsource/geist-mono/500.css';
@import '@fontsource-variable/newsreader';
@import '@fontsource-variable/newsreader/wght-italic.css';
```

font-family 선언의 `'Geist'`→`'Geist Sans'`, `'Newsreader'`→`'Newsreader Variable'`로 교체.

- [ ] **Step 2: `src/layouts/Base.astro`**

```astro
---
import '../styles/global.css';
const { title = 'SLIME LAB' } = Astro.props;
---
<html lang="en"><head>
  <meta charset="utf-8" /><meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
</head><body data-view="hero">
  <div id="grid-bg"></div>
  <div class="frame"><i></i><i></i><i></i><i></i></div>
  <slot />
</body></html>
```

- [ ] **Step 3: 확인 + Commit**

Run: `npm run dev` 후 브라우저 데몬으로 `http://localhost:4321` 스크린샷 → 격자 배경 + 프레임 보임, console 에러 0.
```bash
git add -A && git commit -m "feat: 디자인 토큰 + 그래프페이퍼 글로벌 스타일 + Base 레이아웃"
```

---

### Task 3: rng + heart-state (TDD)

**Files:** Create: `src/engine/rng.ts`, `src/engine/heart-state.ts`, Test: `tests/engine/heart-state.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// tests/engine/heart-state.test.ts
import { describe, it, expect } from 'vitest';
import { mulberry32 } from '../../src/engine/rng';
import { beatCurve, bpmFromCommits, stepHeart, type HeartState } from '../../src/engine/heart-state';

describe('mulberry32', () => {
  it('같은 시드 → 같은 수열', () => {
    const a = mulberry32(42), b = mulberry32(42);
    expect([a(), a(), a()]).toEqual([b(), b(), b()]);
  });
  it('0..1 범위', () => {
    const r = mulberry32(7);
    for (let i = 0; i < 1000; i++) { const v = r(); expect(v).toBeGreaterThanOrEqual(0); expect(v).toBeLessThan(1); }
  });
});

describe('beatCurve', () => {
  it('수축기 피크(t=0.16)는 1.0', () => expect(beatCurve(0.16)).toBeCloseTo(1.0, 5));
  it('이완기(t>=0.46)는 0', () => { expect(beatCurve(0.46)).toBe(0); expect(beatCurve(0.9)).toBe(0); });
  it('전 구간 0..1.05', () => {
    for (let t = 0; t < 1; t += 0.001) { const v = beatCurve(t); expect(v).toBeGreaterThanOrEqual(0); expect(v).toBeLessThanOrEqual(1.05); }
  });
});

describe('bpmFromCommits', () => {
  it('0커밋 → 48 (바닥)', () => expect(bpmFromCommits(0)).toBe(48));
  it('20커밋 → 78', () => expect(bpmFromCommits(20)).toBe(78));
  it('100커밋 → 110 (천장)', () => expect(bpmFromCommits(100)).toBe(110));
});

describe('stepHeart', () => {
  it('proximity로 bpm 최대 1.9배', () => {
    const h: HeartState = { bpm: 60, phase: 0, agit: 1, thump: 0 };
    const { bpmNow } = stepHeart(h, 0.016, 1);
    expect(bpmNow).toBeCloseTo(60 * 1.9, 0);
  });
  it('박동 상승 에지에서 thumpEdge=true 정확히 1회/주기', () => {
    const h: HeartState = { bpm: 60, phase: 0, agit: 0, thump: 0 };
    let edges = 0;
    for (let i = 0; i < 63; i++) { if (stepHeart(h, 1 / 60, 0).thumpEdge) edges++; } // ≈1.05초 = 1주기
    expect(edges).toBe(1);
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `npm test` → Expected: FAIL (모듈 없음)

- [ ] **Step 3: 구현**

```ts
// src/engine/rng.ts
export function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0; a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
```

```ts
// src/engine/heart-state.ts
export interface HeartState { bpm: number; phase: number; agit: number; thump: number; }

export function beatCurve(t: number): number {  // v8 beatCurve 그대로
  if (t < 0.10) return Math.sin(t / 0.10 * Math.PI) * 0.25;
  if (t < 0.16) return (t - 0.10) / 0.06;
  if (t < 0.26) return 1.0 - (t - 0.16) / 0.10;
  if (t < 0.46) return Math.sin((t - 0.26) / 0.20 * Math.PI) * 0.18;
  return 0;
}

export function bpmFromCommits(weekly: number): number {  // 설계서 §6
  return Math.min(110, Math.max(48, 48 + weekly * 1.5));
}

export function stepHeart(h: HeartState, dt: number, proximity: number) {
  const bpmNow = h.bpm * (1 + h.agit * 0.9);
  const prevBeat = beatCurve(h.phase);
  h.phase = (h.phase + dt * bpmNow / 60) % 1;
  const beat = beatCurve(h.phase);
  h.thump = Math.max(0, h.thump - dt * 3.0);
  h.agit += (proximity - h.agit) * Math.min(1, dt * 4);
  return { beat, bpmNow, thumpEdge: prevBeat < 0.5 && beat >= 0.5 };
}
```

- [ ] **Step 4: 통과 확인** — Run: `npm test` → Expected: PASS 전부

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: 엔진 코어 — 시드 RNG, 박동 곡선, BPM 매핑 (TDD)"`

---

### Task 4: anatomy — CSG 점묘 샘플링 (TDD)

**Files:** Create: `src/engine/anatomy.ts`, Test: `tests/engine/anatomy.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// tests/engine/anatomy.test.ts
import { describe, it, expect } from 'vitest';
import { buildHeart } from '../../src/engine/anatomy';
import { mulberry32 } from '../../src/engine/rng';

describe('buildHeart', () => {
  const pts = buildHeart(5000, mulberry32(1));
  it('요청한 점 개수를 정확히 생성', () => expect(pts.count).toBe(5000));
  it('경계 박스 안 (|x|<2, |y|<1.5, |z|<1.8)', () => {
    for (let i = 0; i < pts.count; i++) {
      expect(Math.abs(pts.pos[i * 3])).toBeLessThan(2);
      expect(Math.abs(pts.pos[i * 3 + 1])).toBeLessThan(1.5);
      expect(Math.abs(pts.pos[i * 3 + 2])).toBeLessThan(1.8);
    }
  });
  it('법선은 단위 벡터', () => {
    for (let i = 0; i < 200; i++) {
      const l = Math.hypot(pts.nrm[i * 3], pts.nrm[i * 3 + 1], pts.nrm[i * 3 + 2]);
      expect(l).toBeCloseTo(1, 2);
    }
  });
  it('같은 시드 → 동일 결과 (재현성)', () => {
    const a = buildHeart(100, mulberry32(9)), b = buildHeart(100, mulberry32(9));
    expect(Array.from(a.pos.slice(0, 30))).toEqual(Array.from(b.pos.slice(0, 30)));
  });
  it('색은 0..1', () => {
    for (let i = 0; i < pts.count * 3; i++) { expect(pts.col[i]).toBeGreaterThanOrEqual(0); expect(pts.col[i]).toBeLessThanOrEqual(1); }
  });
});
```

- [ ] **Step 2: 실패 확인** — `npm test` → FAIL

- [ ] **Step 3: 구현 — v8 이식**

v8의 `/* ═══ 해부학적 심장 — CSG 점묘 샘플링 ═══ */` 블록 전체(ELLS/CAPS 정의, insideEll/insideCap/insideOthers, RAMP 3종, seed IIFE, TILT 적용)를 `buildHeart(n, rng)`로 옮긴다. 변경점:
- `Math.random()` 전부 → 인자 `rng()` (재현성 — 테스트 4가 이것을 검증)
- 전역 typed array 대신 함수 내 생성 후 반환:

```ts
export interface AnatomyPoints {
  pos: Float32Array; nrm: Float32Array;
  col: Float32Array;   // ⚠ 기본색(조명 전) 0..1 float — 렌더러에 직접 주지 않음
  size: Float32Array; phase: Float32Array; count: number;
}
export function buildHeart(n: number, rng: () => number): AnatomyPoints { /* v8 seed 로직 */ }
```
**타입 계약:** `anatomy.col`(기본색 float) → mount가 매 프레임 조명 적용 후 **동적 `Uint8Array` COL 버퍼**에 기록 → `renderer.draw(col: Uint8Array)`. v8과 동일한 분리. 혼동 금지.
- 프리미티브 점 배분(`e.n`, `cp.n`)은 비율로 환산: v8 절대값 ÷ 30000 × n (반올림, 잔여는 좌심실 표면으로 — v8 마지막 while과 동일)
- 콜아웃 앵커도 함께 export: `export const ANCHORS = [...]` (v8 COS의 모델 좌표 + TILT 적용 로직 포함)

- [ ] **Step 4: 통과 확인** — `npm test` → PASS

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: 해부학적 심장 CSG 점묘 샘플링 모듈 (TDD, 시드 재현성)"`

---

### Task 5: physics — 스프링/반발/쇼크 (TDD)

**Files:** Create: `src/engine/physics.ts`, Test: `tests/engine/physics.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// tests/engine/physics.test.ts
import { describe, it, expect } from 'vitest';
import { createPhysics, stepPhysics, shock } from '../../src/engine/physics';
import { mulberry32 } from '../../src/engine/rng';

describe('physics', () => {
  it('정지 상태는 그대로', () => {
    const p = createPhysics(10);
    stepPhysics(p, 0.016, null, new Float32Array(20), 78);
    expect(p.ox[0]).toBe(0); expect(p.vx[0]).toBe(0);
  });
  it('오프셋은 스프링으로 0에 수렴', () => {
    const p = createPhysics(1); p.ox[0] = 50;
    for (let i = 0; i < 300; i++) stepPhysics(p, 1 / 60, null, new Float32Array(2), 78);
    expect(Math.abs(p.ox[0])).toBeLessThan(0.5);
  });
  it('커서 반경 안의 점은 커서 반대쪽으로 밀려남', () => {
    const p = createPhysics(1);
    const base = new Float32Array([100, 100]);       // 점 화면좌표 (100,100)
    stepPhysics(p, 0.016, { x: 90, y: 100 }, base, 78); // 커서가 왼쪽 10px
    expect(p.vx[0]).toBeGreaterThan(0);               // 오른쪽으로 가속
  });
  it('shock은 중심에서 바깥 방향 속도를 더함', () => {
    const p = createPhysics(1);
    const base = new Float32Array([200, 100]);
    shock(p, base, 100, 100, 300, mulberry32(3));     // 중심 (100,100), 점은 오른쪽
    expect(p.vx[0]).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: 실패 확인** — `npm test` → FAIL

- [ ] **Step 3: 구현**

```ts
// src/engine/physics.ts  (v8 메인 루프의 물리 항 + shock 이식)
export interface PhysicsState { ox: Float32Array; oy: Float32Array; vx: Float32Array; vy: Float32Array; n: number; }
const K = 46, DAMP = 8;

export function createPhysics(n: number): PhysicsState {
  return { ox: new Float32Array(n), oy: new Float32Array(n), vx: new Float32Array(n), vy: new Float32Array(n), n };
}
/** basePos: 점별 화면 px (x,y 인터리브). mouse=null이면 반발 없음 */
export function stepPhysics(p: PhysicsState, dt: number, mouse: {x:number;y:number}|null, basePos: Float32Array, repelR: number) {
  for (let i = 0; i < p.n; i++) {
    let ax = -p.ox[i] * K - p.vx[i] * DAMP, ay = -p.oy[i] * K - p.vy[i] * DAMP;
    if (mouse) {
      const dx = basePos[i*2] + p.ox[i] - mouse.x, dy = basePos[i*2+1] + p.oy[i] - mouse.y;
      const d2 = dx*dx + dy*dy;
      if (d2 < repelR * repelR) {
        const d = Math.sqrt(d2) + 0.001, f = (repelR - d) / repelR;
        ax += dx / d * f * 5600; ay += dy / d * f * 5600;
      }
    }
    p.vx[i] += ax * dt; p.vy[i] += ay * dt;
    p.ox[i] += p.vx[i] * dt; p.oy[i] += p.vy[i] * dt;
  }
}
export function shock(p: PhysicsState, basePos: Float32Array, cx: number, cy: number, radius: number, rng: () => number) {
  for (let i = 0; i < p.n; i++) {
    const dx = basePos[i*2] - cx, dy = basePos[i*2+1] - cy;
    const d = Math.hypot(dx, dy) + 0.001, f = Math.max(0, 1 - d / radius);
    if (f <= 0) continue;
    p.vx[i] += dx / d * f * (420 + rng() * 520);
    p.vy[i] += dy / d * f * (420 + rng() * 520) - f * 100;
  }
}
```

- [ ] **Step 4: 통과 확인** — `npm test` → PASS
- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: 파티클 물리 — 스프링/커서 반발/쇼크 (TDD)"`

---

### Task 6: renderer + ecg (이식, 시각 검증)

**Files:** Create: `src/engine/renderer.ts`, `src/engine/ecg.ts`

- [ ] **Step 1: `renderer.ts`** — v8 `/* ═══ WebGL ═══ */` 블록 이식:

```ts
export interface PointRenderer {
  resize(): void;
  draw(pos: Float32Array, dep: Float32Array, col: Uint8Array /* mount의 동적 조명색 버퍼 — anatomy.col 아님 */, size: Float32Array, count: number): void;
  readonly dpr: number;
  readonly gl: WebGLRenderingContext | null;   // null = WebGL 미지원 (폴백 신호)
}
export function createPointRenderer(canvas: HTMLCanvasElement): PointRenderer { /* v8 VSH/FSH/attr/버퍼 로직 */ }
```
변경점: 컨텍스트 획득 실패 시 throw 대신 `gl: null` 리턴(폴백은 mount에서 처리). DPR cap 1.5 유지. `bufferSubData` 4종 업로드 + draw를 `draw()`로 캡슐화.

- [ ] **Step 2: `ecg.ts`** — v8 ECG 부분 이식:

```ts
export function createEcg(canvas: HTMLCanvasElement) {
  return { push(value: number, dt: number): void, draw(): void };
}
```
shadowBlur 금지 — v8의 이중 스트로크(6px 25% + 2px 100%) 방식 유지 (설계서 §5 성능 교훈).

- [ ] **Step 3: 타입 체크** — Run: `npx astro check && npx tsc --noEmit -p .` → Expected: 에러 0
- [ ] **Step 4: Commit** — `git add -A && git commit -m "feat: WebGL 포인트 렌더러 + ECG 모듈 이식"`

---

### Task 7: mount 오케스트레이터 + cursor/overlay 이식

**Files:** Create: `src/engine/mount.ts`, `src/engine/cursor.ts`, `src/engine/overlay.ts`

> 사운드는 설계서 §11에 따라 **M3** — M1에서 이식하지 않음 (리뷰 반영). v8의 thumpEdge 신호만 mount에서 noop 훅(`onBeat?: () => void`)으로 열어둔다.

- [ ] **Step 1: 보조 모듈 2개 이식** — v8 블록 그대로, DOM 참조는 인자로 주입:
  - `cursor.ts`: `initCursor(dotEl, ringEl)` → `{ step(dt, mouse, pokeActive), bindHover(root) }` (v8 "커서"+"자석" 블록. 자석은 `initMagnetics(root)`)
  - `overlay.ts`: `initOverlay(annoCanvas, calloutEls, railEl, kickerEl)` → `{ draw(transform, heroVisible), clampRail() }` (v8 "주석 캔버스" + "레일 클램프" 블록. DUST 130개 포함. **레일의 표본 수 표기는 하드코딩 금지 — 데이터 길이 기반**)

- [ ] **Step 2: `mount.ts`** — v8 "메인 루프"를 모듈 조립으로 재구성:

```ts
export interface MountOpts {
  host: HTMLElement;                  // 히어로 섹션 루트 (data-* 셀렉터로 내부 요소 탐색)
  pointCount?: number;                // 기본 30000, 모바일 12000
  bpm: number;                        // vitals에서 계산된 값
  reducedMotion?: boolean;
}
export function mountHeart(opts: MountOpts): { destroy(): void } | { fallback: true }
```
구성: buildHeart(시드 20260611 고정) → createPointRenderer(gl null이면 `{fallback:true}` 리턴) → rAF 루프에서 stepHeart → 투영(스윙 ±34°, v8 변환식) → stepPhysics → 조명/색/크기 계산 → renderer.draw → ecg → overlay → cursor. `reducedMotion`이면 1프레임 렌더 후 루프 정지. `destroy()`는 rAF 취소 + 리스너 해제.
모바일 판정: `matchMedia('(pointer: coarse)')` → pointCount 12000, 커서 비활성, 탭=쇼크.

- [ ] **Step 3: 타입 체크** — `npx tsc --noEmit -p .` → 에러 0
- [ ] **Step 4: Commit** — `git add -A && git commit -m "feat: 히어로 엔진 오케스트레이터 + 커서/오버레이/사운드 모듈"`

---

### Task 8: 표본 콘텐츠 컬렉션 + 정적 바이탈

**Files:** Create: `src/content.config.ts`, `src/content/specimens/*.md` (12개), `src/data/vitals.json`, `src/lib/vitals.ts`, Test: `tests/engine/vitals.test.ts`

- [ ] **Step 1: 스키마**

```ts
// src/content.config.ts
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';
const specimens = defineCollection({
  loader: glob({ pattern: '**/*.md', base: './src/content/specimens' }),
  schema: z.object({
    no: z.string(), name: z.string(),
    class: z.enum(['consumer app','creator tool','dev tool','desktop tool','experiment']),
    year: z.number(), status: z.enum(['live','beta','frozen','archived']),
    repo: z.string().nullable().default(null),
    links: z.object({ site: z.string().optional(), store: z.string().optional() }).default({}),
    autopsy: z.object({ died: z.string(), cause: z.string(), organs: z.string() }).nullable().default(null),
  }),
});
export const collections = { specimens };
```

- [ ] **Step 2: 표본 16개 작성** (설계서 §2 "16개+" 충족 — 리뷰 반영) — v8 SPECIMENS 12개(weeple, muse-prompt-studio, srt-saas, tokodachi, rightbefore, kage-studio, beep-get, orido, receipt-anything, flatsnap, insane-search, diffwatch) + 추가 4개:
  - `searchmachine` — dev tool, 2026, **archived**, autopsy: `{ died: "2026-04", cause: "economics — unit cost never closed", organs: "crawl heuristics → insane-search" }`
  - `youtube-shorts-music` — creator tool, 2026, beta (Suno+Meta 생성기 실사용)
  - `ai-threads` — creator tool, 2026, beta (Threads 자동 포스팅 파이프라인)
  - `mailcal` — experiment, 2026, **frozen** (PoC 1단계 중단)
  본문 설명은 v8의 `d` 필드/메모리 요약 기반 1-2문장 (M2에서 케이스 스터디로 확장). diffwatch autopsy: `{ died: "2026-05", cause: "scope creep, weak retention loop", organs: "diff engine → srt-saas" }`. 카피는 추정 금지 — 불확실한 수치는 쓰지 않는다.

- [ ] **Step 3: 바이탈 (실패 테스트 → 구현)**

```ts
// tests/engine/vitals.test.ts
import { describe, it, expect } from 'vitest';
import { labBpm } from '../../src/lib/vitals';
it('weekly 합산으로 lab BPM 계산', () => {
  expect(labBpm({ weeple: { weeklyCommits: 10 }, muse: { weeklyCommits: 10 } } as never)).toBe(78); // 48+20*1.5
});
```

```json
// src/data/vitals.json — M2 Worker 출력과 동일 스키마. ⚠ 16개 표본 키 전부 포함 (리뷰 반영)
{ "generatedAt": "2026-06-11T00:00:00Z",
  "specimens": { "weeple": { "weeklyCommits": 14, "lastShipDays": 2 },
                 "muse-prompt-studio": { "weeklyCommits": 8, "lastShipDays": 6 },
                 "...": "나머지 14개 슬러그 전부 — archived/frozen은 weeklyCommits 0" } }
```

```ts
// src/lib/vitals.ts
import { bpmFromCommits } from '../engine/heart-state';
import raw from '../data/vitals.json';
export interface Vitals { generatedAt: string; specimens: Record<string, { weeklyCommits: number; lastShipDays: number }>; }
export function labBpm(specimens: Vitals['specimens']): number {
  const total = Object.values(specimens).reduce((s, v) => s + v.weeklyCommits, 0);
  return bpmFromCommits(total);
}
export function getVitals(): { data: Vitals; stale: boolean } {
  const data = raw as Vitals;
  return { data, stale: false };
  // M1은 정적 스냅샷 — stale 판정은 M2 라이브 fetch 도입 시 활성화 (리뷰 반영: 정적 데이터가
  // 24h 후 "고장"처럼 보이는 자폭 방지). 대신 UI에 "DATA AS OF <generatedAt 날짜>" 상시 표기 = 정직함 유지.
}
```

- [ ] **Step 4: 검증 + Commit** — `npm test` PASS, `npm run build` 성공.
`git add -A && git commit -m "feat: 표본 컬렉션(12종) + 정적 바이탈 + lab BPM 계산 (TDD)"`

---

### Task 9: index.astro — §1 HERO 통합

**Files:** Create: `src/pages/index.astro`, Modify: `src/layouts/Base.astro`(커서 요소)

- [ ] **Step 1: 마크업** — v8 `#view-hero` 내부(topbar/headline/vitals/bottom/rail/callouts/canvas 2장)를 Astro 마크업으로. 텍스트 스태거/스크램블은 v8 "헤드라인"/"스크램블" 블록을 `<script>`(Astro 클라이언트 스크립트)로 이식. 엔진 연결:

```astro
<script>
  import { mountHeart } from '../engine/mount';
  import { getVitals, labBpm } from '../lib/vitals';
  const { data } = getVitals();
  const reduced = matchMedia('(prefers-reduced-motion: reduce)').matches;
  const r = mountHeart({ host: document.getElementById('hero')!, bpm: labBpm(data.specimens), reducedMotion: reduced });
  if ('fallback' in r) document.getElementById('hero')!.classList.add('no-webgl');  // 정적 PNG 표시
</script>
```

- [ ] **Step 2: WebGL 폴백 이미지** — 브라우저 데몬으로 v8 심장 부분 캡처 → `public/fallback-heart.png`. CSS: `.no-webgl .hero-canvas{display:none} .no-webgl .hero-fallback{display:block}`

- [ ] **Step 2.5: 데이터 시점 라벨** (리뷰 반영) — vitals 영역 마지막 행에 `DATA AS OF <generatedAt의 YYYY-MM-DD>` 모노 라벨 렌더. M2에서 stale 로직으로 교체될 자리.

- [ ] **Step 3: 시각 검증 (필수 통과 기준)**

브라우저 데몬: `goto http://localhost:4321` →
- console 에러 0
- 스크린샷: 심장 렌더 + 콜아웃 4개 + 레일 + ECG 확인
- `viewport 1280x680` 재확인: 레일이 키커 침범 없음 (+N more 접힘)
- 커서 이동 주입 후 BPM 텍스트 상승 확인

- [ ] **Step 4: Commit** — `git add -A && git commit -m "feat: 히어로 섹션 — 점묘 심장 엔진 통합 + WebGL 폴백"`

---

### Task 10: §2 SPECIMEN INDEX + 검사실 스텁

**Files:** Modify: `src/pages/index.astro`, Create: `src/pages/specimen/[slug].astro`

- [ ] **Step 1: 인덱스 섹션** — v8 `.srow` 마크업을 콘텐츠 컬렉션 기반 정적 렌더로 (JS 생성 금지 — Astro 빌드 타임):

```astro
{specimens.map((s) => (
  <a class="srow" href={`/specimen/${s.id}`}>
    <span class="no">LAB—{s.data.no}</span><span class="nm">{s.data.name.toUpperCase()}</span>
    <span class="cls">{s.data.class}</span><span class="yr">{s.data.year}</span>
    <span class={`stx st-${s.data.status === 'live' ? 'live' : s.data.status === 'archived' ? 'arch' : 'beta'}`}><i></i>{s.data.status.toUpperCase()}</span>
    <span class="go">EXAMINE →</span>
  </a>
))}
```
정렬: `no` 오름차순. 시맨틱: 섹션 헤딩 `<h2>` + 행은 `<a>` (설계서 §9).

- [ ] **Step 2: 검사실 스텁** — `[slug].astro`: `getStaticPaths`로 16페이지. v8 `#view-detail` 레이아웃(dt-no/dt-name/dt-meta/본문/뒤로가기). archived/frozen이면 autopsy/동결 블록(†) 렌더.
**ClientRouter(View Transitions)는 설계서 §11대로 M2 — M1은 일반 MPA 링크** (리뷰 반영: SPA 네비 시 mount 생명주기 처리가 필요해지는데 그건 M2 범위. M2에서 `astro:page-load` mount + `astro:before-swap` destroy와 함께 도입).

- [ ] **Step 3: 시각 검증** — 데몬: 인덱스 행 클릭 → 검사실 이동 → 브라우저 back으로 복귀 → **심장이 다시 정상 렌더되는지 확인** (MPA full reload라 자동 보장되지만 명시 검증). 16개 중 4개 샘플(weeple, tokodachi, diffwatch—부검, searchmachine—부검). console 에러 0.

- [ ] **Step 4: Commit** — `git add -A && git commit -m "feat: 표본 인덱스(빌드타임 렌더) + 검사실 스텁 12페이지"`

---

### Task 11: 성능/접근성 패스 (M1 수준)

**Files:** Modify: `src/pages/index.astro`, `src/styles/global.css`

- [ ] **Step 1: Lighthouse** — `npm run build` 후 preview를 **백그라운드로** 띄우고 (리뷰 반영: 포그라운드 블로킹 + Git Bash `/tmp` 경로 변환 함정):
```bash
npm run preview &   # 또는 run_in_background
npx lighthouse http://localhost:4321 --quiet --chrome-flags="--headless" --output=json --output-path=./artifacts/lh.json
node -e "const r=require('./artifacts/lh.json').categories; console.log(Object.entries(r).map(([k,v])=>k+': '+Math.round(v.score*100)).join('\n'))"
```
(`artifacts/`는 .gitignore에 추가)
Expected: Performance/A11y/Best Practices/SEO 모두 95+. 미달 항목은 리포트 보고 수정(이미지 preload, meta description 등) — 단 점 수 축소로 디자인을 깎는 수정은 금지, 보고 후 결정.

- [ ] **Step 2: reduced-motion 확인** — 데몬에서 `emulateMediaFeatures`가 없으므로 mount 옵션 강제 플래그(`?reduced=1` 쿼리 지원 추가)로 1프레임 정적 렌더 확인.

- [ ] **Step 3: Commit** — `git add -A && git commit -m "perf: Lighthouse 95+ 패스 + reduced-motion 폴백"`

---

### Task 12: GitHub 레포 + Cloudflare Pages 배포

- [ ] **Step 1: 원격 레포** — 이름은 가칭(설계서 오픈 아이템 1, 이후 rename 가능):
```bash
gh repo create slimex200-wq/slime-lab --public --source . --push
```

- [ ] **Step 2: CF Pages 배포**
```bash
npm run build
npx wrangler pages deploy dist --project-name slime-lab
```
wrangler 미인증이면 형에게 `! npx wrangler login` 안내 (세션 내 실행). 산출 URL(`slime-lab.pages.dev`) 기록.

- [ ] **Step 3: 배포 검증 (필수)** — 데몬으로 라이브 URL 접속: 심장 렌더 + console 에러 0 + 인덱스→검사실 네비. 스크린샷 보고.

- [ ] **Step 4: Commit + 보고** — README.md에 배포 URL/명령 기록 후 커밋. M1 완료 보고 (배포 URL + 스크린샷 + 테스트/Lighthouse 수치).

---

## Self-Review 체크 (작성 후 수행 완료)

- 스펙 커버리지: §1·§2(T9·T10), 히어로 엔진 §5(T3~7), 데이터 §6 정적분(T8), 스택 §8(T1), 접근성 §9 M1분(T9 폴백·T11), 성능 §10(T11), 배포(T12). THE DIG·터미널·사운드 ON 기본·OG = M2/M3 — 스펙 마일스톤과 일치.
- 플레이스홀더: 코드 필요한 스텝은 코드 포함, 이식 스텝은 v8 블록명 + 시그니처 + 변경점 명시.
- 타입 일관성: `bpmFromCommits`(T3) ↔ `labBpm`(T8) ↔ `mountHeart.bpm`(T9) 연결 확인.
