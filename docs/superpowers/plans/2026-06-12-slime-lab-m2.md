# SLIME LAB M2 — 살아나기 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. 체크박스로 추적.

**Goal:** 심장이 진짜 데이터로 뛰게 한다 — CF Worker cron이 GitHub 활동을 집계해 BPM·바이탈이 실시간이 되고, 검사실 16페이지에 팩트체크된 케이스 스터디가 들어가고, View Transitions로 페이지 전환이 이어진다.

**Architecture:** `workers/vitals` (CF Worker, cron 15분 → GitHub API → KV → CORS JSON 엔드포인트). 사이트는 빌드 시 정적 vitals를 굽고(폴백 + SEO), 클라이언트에서 Worker를 fetch해 DOM/엔진을 패치. 실패 시 baked 데이터 + "TELEMETRY STALE" 라벨 (silent fallback 금지). M1 컨벤션(TDD·시각 검증·`git add -A`·astro check) 그대로.

**전제 (이 세션에서 검증 완료):**
- 레포 매핑 11종 확정: tokodachi, srt-saas, kage-studio, beep-get, orido, receipt-anything, flatsnap, ai-threads, diffwatch, searchmachine, rightbefore→`rightbefore-releases`(데스크톱 앱 레포 미발견 — 릴리스 피드가 ship 신호로 적합)
- **미매핑 5종 (repo:null 유지, 추측 금지)**: weeple, muse-prompt-studio, insane-search, mailcal, youtube-shorts-music → 형 확인 후 채움. Worker는 null을 건너뛰고 해당 표본은 baked 값 표시
- GitHub API 인증: Worker secret `GITHUB_TOKEN` 필요 (private 레포 9종). **fine-grained PAT(contents:read, metadata:read)를 형이 직접 발급** — secret 값은 어떤 파일/로그에도 기재 금지(P0). 미설정 시 Worker는 public 레포만 집계하고 응답에 `"auth":"degraded"` 명시

---

### Task 1: 표본 frontmatter repo 매핑

**Files:** Modify: `src/content/specimens/*.md` (11개), Delete: `scripts/gen-specimens.mjs`

- [ ] Step 1: 11개 md의 `repo: null` → `repo: "slimex200-wq/<확정 레포>"` (rightbefore는 `slimex200-wq/rightbefore-releases`)
- [ ] Step 2: gen 스크립트 삭제 — md가 이제 SoT (재실행하면 수기 카피를 덮어쓰므로)
- [ ] Step 3: `npm run build` 통과 확인 → `git add -A && git commit -m "feat: 표본 레포 매핑 11종 + 생성 스크립트 은퇴"`

### Task 2: vitals Worker — 순수 로직 (TDD)

**Files:** Create: `workers/vitals/src/logic.ts`, Test: `tests/worker/logic.test.ts`

- [ ] Step 1: 실패 테스트 — `tests/worker/logic.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { daysSince, buildVitals, type RepoFetcher } from '../../workers/vitals/src/logic';

describe('daysSince', () => {
  it('2일 전 ISO → 2', () => {
    const now = new Date('2026-06-12T00:00:00Z');
    expect(daysSince('2026-06-10T00:00:00Z', now)).toBe(2);
  });
});

describe('buildVitals', () => {
  const fetcher: RepoFetcher = async (repo) => {
    if (repo === 'o/a') return { weeklyCommits: 7, pushedAt: '2026-06-11T00:00:00Z' };
    throw new Error('api down');
  };
  it('성공 레포는 집계, repo:null은 건너뜀', async () => {
    const out = await buildVitals(
      [{ slug: 'a', repo: 'o/a' }, { slug: 'b', repo: null }],
      fetcher, new Date('2026-06-12T00:00:00Z'),
    );
    expect(out.specimens['a']).toEqual({ weeklyCommits: 7, lastShipDays: 1 });
    expect(out.specimens['b']).toBeUndefined();
    expect(out.errors).toEqual([]);
  });
  it('개별 레포 실패는 errors에 기록하고 계속 (silent fallback 금지)', async () => {
    const out = await buildVitals([{ slug: 'x', repo: 'o/fail' }], fetcher, new Date());
    expect(out.errors).toEqual(['x']);
  });
});
```

- [ ] Step 2: RED 확인 → Step 3: 구현

```ts
// workers/vitals/src/logic.ts
export interface RepoVital { weeklyCommits: number; pushedAt: string; }
export type RepoFetcher = (repo: string) => Promise<RepoVital>;
export interface VitalsOut {
  generatedAt: string;
  specimens: Record<string, { weeklyCommits: number; lastShipDays: number }>;
  errors: string[];
}
export function daysSince(iso: string, now: Date): number {
  return Math.max(0, Math.floor((now.getTime() - new Date(iso).getTime()) / 86400000));
}
export async function buildVitals(
  map: { slug: string; repo: string | null }[], fetcher: RepoFetcher, now: Date,
): Promise<VitalsOut> {
  const out: VitalsOut = { generatedAt: now.toISOString(), specimens: {}, errors: [] };
  for (const { slug, repo } of map) {
    if (!repo) continue;
    try {
      const v = await fetcher(repo);
      out.specimens[slug] = { weeklyCommits: v.weeklyCommits, lastShipDays: daysSince(v.pushedAt, now) };
    } catch { out.errors.push(slug); }
  }
  return out;
}
```

- [ ] Step 4: GREEN → 커밋 `feat: vitals worker 순수 로직 (TDD)`

### Task 3: vitals Worker — 핸들러 + 배포 설정

**Files:** Create: `workers/vitals/src/index.ts`, `workers/vitals/wrangler.toml`

- [ ] Step 1: `index.ts` — scheduled: REPO_MAP(표본 frontmatter와 동일한 11종 하드코딩 상수) → `githubFetcher(env.GITHUB_TOKEN)` (per repo: `GET /repos/{r}` → pushed_at, `GET /repos/{r}/commits?since=<7d>&per_page=100` → 배열 길이, UA 헤더 필수, 토큰 없으면 Authorization 생략) → buildVitals → `env.VITALS.put('latest', JSON.stringify({...out, auth: token?'ok':'degraded'}))`. fetch: KV get → 200 JSON, 헤더 `access-control-allow-origin:*`, `cache-control:public,max-age=60`. KV 비었으면 503 `{error:'no data yet'}`
- [ ] Step 2: `wrangler.toml` — name `slime-lab-vitals`, `[triggers] crons = ["*/15 * * * *"]`, `[[kv_namespaces]] binding = "VITALS"` (id는 `npx wrangler kv namespace create VITALS` 출력으로 채움)
- [ ] Step 3: 배포 + 시크릿:
```bash
cd workers/vitals && npx wrangler deploy
# GITHUB_TOKEN은 형이 직접: ! cd ~/claude-projects/lab/workers/vitals && npx wrangler secret put GITHUB_TOKEN
npx wrangler triggers deploy 2>/dev/null || true   # cron 활성 확인
curl -s https://slime-lab-vitals.<subdomain>.workers.dev | head -c 300   # scheduled 1회 수동: wrangler dev --test-scheduled 또는 대시보드
```
- [ ] Step 4: 커밋 `feat: vitals worker 핸들러 + cron/KV 설정`

### Task 4: 클라이언트 라이브 연결

**Files:** Create: `src/lib/vitals-client.ts`, Modify: `src/engine/mount.ts`(setBpm 추가), `src/pages/index.astro`, `src/pages/specimen/[slug].astro`, `src/styles/global.css`

- [ ] Step 1: mount 반환에 `setBpm(n: number)` 추가 (heart.bpm 갱신)
- [ ] Step 2: `vitals-client.ts` — `fetchLive(url, timeoutMs=3000)`: AbortController, 실패/`auth:degraded`+빈 specimens면 `null`. 성공 시:
  - `#v-bpm`용 bpm 재계산(labBpm) → `window.__heart?.setBpm(bpm)`
  - `[data-vital-slug]` 요소들 패치 (인덱스 행 `--pulse-dur` CSS 변수 = `clamp(0.8, 3 - weekly*0.15, 3)`s, 상세 LAST SHIP 텍스트)
  - 실패 시: `#telemetry-label`을 `TELEMETRY STALE`로 교체 + amber 클래스
- [ ] Step 3: index.astro — 행에 `data-vital-slug={s.id}`, vitals 영역 `DATA AS OF` 라벨에 `id="telemetry-label"`, 스크립트에서 `fetchLive(import.meta.env.PUBLIC_VITALS_URL)` 호출 (`.env`에 PUBLIC_VITALS_URL — 비밀 아님, Worker 공개 URL). `st-live i { animation-duration: var(--pulse-dur, 1.6s) }` CSS 연결
- [ ] Step 4: 시각 검증 — 데몬: Worker URL 차단(존재하지 않는 URL로 일시 변경) 시 STALE 라벨 노출, 정상 시 BPM 변경 확인 → 커밋 `feat: 라이브 바이탈 클라이언트 연결 + STALE 폴백`

### Task 5: View Transitions + 엔진 생명주기

**Files:** Modify: `src/layouts/Base.astro`, `src/pages/index.astro`

- [ ] Step 1: Base.astro head에 `import { ClientRouter } from 'astro:transitions'` + `<ClientRouter />`
- [ ] Step 2: index.astro 스크립트 재구성 — 모듈 top-level 실행 금지:
```ts
let engine: { destroy(): void } | null = null;
function boot() { /* mountHeart + 스태거 + 스크램블 + fetchLive — #hero 없으면 return */ }
document.addEventListener('astro:page-load', boot);
document.addEventListener('astro:before-swap', () => { engine?.destroy(); engine = null; });
```
- [ ] Step 3: 데몬 E2E — index→detail→back(VT)→**심장 재마운트 + rAF 단일 루프 확인**(`performance`로 중복 루프 감지: destroy 후 카운터 정지), console 에러 0 → 커밋 `feat: View Transitions + 엔진 mount/destroy 생명주기`

### Task 6: 케이스 스터디 카피 16종

**Files:** Modify: `src/content/specimens/*.md` 본문 전부

- [ ] Step 1: 팩트 수집 — 병렬 서브에이전트로 매핑된 레포 README/최근 릴리스 + 로컬 디렉터리(tokodachi=`~/tokodachi`, beep-get=`~/claude-projects/beep-get` 등 메모리 경로) 조사. **수치는 소스에서 확인된 것만, 추정 금지** (기존 피드백: 홍보 카피는 소스 기반 팩트체크)
- [ ] Step 2: 본문 작성 — 구조: `What it is`(1문단) / `The experiment`(문제→접근 1-2문단) / `Findings`(결과·상태 1문단). 시그니처 5종(weeple, muse, srt-saas, tokodachi, rightbefore)은 더 깊게. archived는 부검 톤 유지. 영어, AI 과장 카피 금지
- [ ] Step 3: `npm run build` + 데몬으로 3페이지 샘플 확인 → 커밋 `content: 검사실 케이스 스터디 16종 (소스 팩트체크)`

### Task 7: 검증 패스 + 배포

- [ ] Step 1: `npm test`(전체) + `npx astro check` + Lighthouse 재측정(프리뷰 포트 주의 — M1 함정) → perf 회귀 없는지 (90 미만이면 보고)
- [ ] Step 2: `npm run build && npx wrangler pages deploy dist --project-name slime-lab` → 라이브에서 BPM 실데이터 확인 (Worker 응답과 대조)
- [ ] Step 3: README 갱신(M2 완료, Worker URL/시크릿 절차) → 커밋/푸시 → M2 완료 보고 (미매핑 5종 + PAT 발급 요청 포함)

---

## Self-Review
- 스펙 §6 전 항목 매핑: cron 15분(T3), BPM 매핑(기존 labBpm 재사용, T4), 인덱스 맥박 동일 데이터(T4 pulse-dur), 실패 모드 STALE(T4)+errors 필드(T2). OG 동적 이미지는 스펙상 M3 — 제외 정당.
- 타입: RepoVital/VitalsOut(T2) ↔ index.ts(T3) ↔ vitals-client(T4) 일관. mount setBpm(T4-1)을 T4-2가 사용.
- 리스크 명시: 미매핑 5종, PAT 미발급 시 degraded 모드, weeple(시그니처)의 라이브 데이터 부재는 형 입력 전까지 한계.
